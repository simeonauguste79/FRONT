alert("LES OPERATEURS");
/*L'operateur ET : && ou AND
--Si je cumule 2 cconditions:
=> prénoms === prenomlogin
=> monAge === agelogin
Pour que les 2 correspondent pour être validées, il faut les associer if((prenomlogin === prenom) && (agelogin === monAge)){
    /*--code---*/
    /*----code----*/
    //Je ne rentre ici que les 2 conditions vraies
    //}
    /*
    if((A) && (B)) {....}
    =< si A est faux et b est VRAI => FAUX
    => si A est vrai et B est FAUX => FAUX
    => si A est FAUX et B est FAUX => FAUX
    => si A VRAI et B est VRAI => VRAI*/
    /*
    L'operateur OU || ou OR (| pipe => alt gr + 6)
    if((c) || (D)){.......}
    => si C est FAUX et D est FAUX => FAUX
    => si C est FAUX et D est VRAI => VRAI
    => si C est VRAI et D est FAUX => FAUX
    => si C est VRAI et D est VRAI => FAUX*/
    
    /*L'operateur "!" signifie : le contraire de var utilisateurLog = true;
    if(!utilisateurLog){
     //si l'utlisateur n'est pas logé.   
    }
    //ce qui revient à écrire :
    if(utilisateurLog == false){
    //si l'utilisateur n'est pas logé.
    }
    */
